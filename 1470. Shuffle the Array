class Solution {
public:
    vector<int> shuffle(vector<int>& nums, int n) {
        vector<int> another;
        for(int i=0,j=n; i<n; i++,j++){
            
            another.push_back(nums[i]);
            another.push_back(nums[j]);

        }
        return another;
    }
};
